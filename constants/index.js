const abi = require("../constants/abi.json")
const abiEcommerce = require("../constants/abiEcommerce.json")
const abiEcommerceMarket = require("../constants/abiEcommerceMarket.json")

const contractAddresses = require("../constants/contract.json")
const addressEcommerce = require("../constants/addressEcommerce.json")
const addressEcommerceMarket = require("../constants/addressEcommerceMarket.json")

module.exports = {
    contractAddresses,
    addressEcommerce,
    addressEcommerceMarket,
    abi,
    abiEcommerce,
    abiEcommerceMarket,
}
